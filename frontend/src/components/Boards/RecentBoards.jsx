import styled from "styled-components";

const RecentBoardsWrapper = styled.div`



`;

const RecentBoards = () => {
    return (
        <RecentBoardsWrapper>
            RecentBoards
        </RecentBoardsWrapper>
    );
};

export default RecentBoards;
